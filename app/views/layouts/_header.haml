.pull-left.logo= image_tag('icons/header-astroboy.png')

.menu
  .title= link_to 'SKARADAMS', root_path

  .menu-items
    .item= link_to 'blog', root_path, class: ("active" if is_active('home/index', params))
    .item= link_to 'qui suis-je ?', about_path, class: ("active" if is_active('home/about', params))
    .item= link_to 'contact', contact_path, class: ("active" if is_active('home/contact', params))
